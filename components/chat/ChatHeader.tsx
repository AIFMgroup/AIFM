'use client';

import { ReactNode } from 'react';

/**
 * Chat header bar. Used by FullscreenChatPage; implementation
 * can be moved here when refactoring the main page.
 */
export function ChatHeader({ children }: { children?: ReactNode }) {
  return <>{children}</>;
}
