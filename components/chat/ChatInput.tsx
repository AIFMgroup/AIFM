'use client';

import { ReactNode } from 'react';

/**
 * Chat input area (text field, attach, send). Used by FullscreenChatPage;
 * implementation can be moved here when refactoring.
 */
export function ChatInput({ children }: { children?: ReactNode }) {
  return <>{children}</>;
}
