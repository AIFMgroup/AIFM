'use client';

import { ReactNode } from 'react';

/**
 * Scrollable chat message list. Used by FullscreenChatPage;
 * implementation can be moved here when refactoring.
 */
export function ChatHistory({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
